# @pixel-wall/shared

Types and constants shared between the backend and the frontend: the wire format
of the pixel wall state, API request/response shapes and wall dimensions.

Consumed directly as TypeScript source by both workspaces — no build step.
